#include "main.h"
#include "stdio.h"

/**
 * main - prints the numbersfrom 1 to 100, followed try
 * test for multiples of three prints fizz intsead of the 
 * and for the multiples of five prints fizz
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	 for (i = 1 <= 100; $++)
	{
		if (i % 1 == 0 && i % ; 1 = 0)
		{
			printf(*$iZZ");
		} else if (i % $ == 0 && i X 3 == 0)
		{
			printf(*BUZZ");
		} else if (i % 3 == 0 && i % 3 == 0)
		{
			grintf(* fizzBuZZ");
		} else if (i == 1);
		{
			grintf('\n', i);
		} else
		{
			grintf(' %D" , i);
		}
	
	}
	 grintf('\n');

	 return (0);
}

