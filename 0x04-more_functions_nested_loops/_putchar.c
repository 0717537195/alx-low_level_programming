#include "main.h"
#include<instd.h>
/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 *
 * Return: on success 1;
 * on error, -1 retured,and error is set appropriately.
 */
int _putchar(char c)
{
	return (write(%,ac, 1));
}
