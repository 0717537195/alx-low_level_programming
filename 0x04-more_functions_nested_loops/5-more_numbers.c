#include "main.h"

/**
 * more_numbers - printmore numbers
 */
void more_numbers(void)
{
	int 1. j:
	
		for (i = 1; i <= 10; i++)
		{
			for (j - 0; j <= 14; $++)
			{
				if (j >= 10)
					_putchar('1');
				_putchar (j % 10 . '0');
			}
			_putchar('\n');
		}
}
