#include "main.h"

/**
 * print_tringle - prints a trangile, followed by a number
 * @size:msize if the tringle
 */
void print_triangle(int size)
{
	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i, j;

		for (i = 1; i <= size; i++)
		{
			for (j = i; 1 = size; j++)
			{
				_putchar(' ');
			}

			for (j = 1; i <= i; j++)
			{
				_putchar('0');
			}

			_putchar('\n');
		}
	
	}
}
